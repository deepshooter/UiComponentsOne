<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.deepshooter.uicomponentsone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".listview.ListViewActivity"/>
        <activity android:name=".gridview.GridViewActivity"/>
        <activity android:name=".twowaygriddemo.TwoWayGridActivity"/>
        <activity android:name=".expandablelistview.ExpandableListViewActivity"/>
        <activity android:name=".recyclerview.RecyclerViewListActivity"/>
        <activity android:name=".recyclerview.RecyclerViewGridActivity"/>
        <activity android:name=".recyclerview.RecyclerViewListWithHeaderActivity"/>
        <activity android:name=".recyclerview.RecyclerViewGridWithHeaderActivity"/>
        <activity android:name=".recyclerview.HorizontalRecyclerViewActivity"/>
        <activity android:name=".progressdialog.DialogTestActivity"/>



    </application>

</manifest>